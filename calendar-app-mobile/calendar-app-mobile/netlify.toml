# netlify.toml
[build]
  base = "calendar-app-mobile/calendar-app-mobile/calendar-app-mobile"
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
